@page "/test"
@using SharedKernel.Contracts.v1.Champions.Dtos
@using WebApp.Champions.Augments

<PageTitle>Test</PageTitle>

<Augment Model="@Model" 
         Update="() => Update(Model)" 
         Remove="() => Remove(Model)" />

@code{
    private ChampionAugmentDto Model = new("", "Death Mark", "Talent", "#FF0000");

    private static void Update(ChampionAugmentDto model) => Console.WriteLine(model.AugmentName);
    private static void Remove(ChampionAugmentDto model) => Console.WriteLine(model.AugmentTarget);
}